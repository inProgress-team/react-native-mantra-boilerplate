const { describe, it } = global;
import { expect } from 'chai';
import { spy, stub } from 'sinon';
import { composer, depsMapper } from '../login';

describe('user.containers.login', () => {
  describe('composer', () => {
    it('should ', () => {

    });
  });
});
